export default function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <h1>Editon Fresh Start</h1>
        <p>
          Frontend has been reset. Build the new asset-loading experience from
          this clean baseline.
        </p>
      </section>
    </main>
  );
}
